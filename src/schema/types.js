// This file previously contained TypeScript interfaces which are not needed in JavaScript.
// JavaScript uses duck typing and doesn't require interface definitions.
// The types have been removed as they are compile-time constructs only.

// Exporting empty object for compatibility if any imports expect it
module.exports = {};